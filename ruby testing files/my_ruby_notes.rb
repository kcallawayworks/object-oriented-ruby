# ruby notes

# array = []
# hash = {} can be expressed like this as a string 
# {first_name: "Kelly"}
# "#{employee2[:first_name]}"

class Alwaysneesds caps
end

initialize(enter values that will be passed) - in a class get calls when a class is called.
end

@shows and instance variable - these will retain the memory when shown inside class